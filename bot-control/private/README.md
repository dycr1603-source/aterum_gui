# Private runtime material

Esta carpeta existe solo como marcador local y esta ignorada por Git. Puede contener referencias operativas cifradas, pero nunca secretos en texto plano destinados al repositorio.

Usar un gestor de secretos o un backup cifrado para credenciales, `.env`, bases SQLite y exports raw de n8n.
